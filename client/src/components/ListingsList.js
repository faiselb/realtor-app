import React from 'react'
import styled from 'styled-components'

function ListingsList (props) {

    props.community.listings.map((listing, index) => {
        return (<div key={index}>div</div>)
    }

    return (
        <div>
            test
        </div>
    )
}

